import 'package:flutter/material.dart';

class ClipTagsView extends StatelessWidget {
  const ClipTagsView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
     child: Padding(padding: EdgeInsets.all(5),)
    );
  }
}